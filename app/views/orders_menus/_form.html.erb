<%= form_with(model: orders_menu) do |form| %>
  <% if orders_menu.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(orders_menu.errors.count, "error") %> prohibited this orders_menu from being saved:</h2>

      <ul>
        <% orders_menu.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :order_id, style: "display: block" %>
    <%= form.number_field :order_id %>
  </div>

  <div>
    <%= form.label :menu_id, style: "display: block" %>
    <%= form.text_field :menu_id %>
  </div>

  <div>
    <%= form.label :integer, style: "display: block" %>
    <%= form.text_field :integer %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
